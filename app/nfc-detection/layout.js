export default function NFCLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>NFC Detection - Rizzler</title>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </head>
      <body>{children}</body>
    </html>
  );
}
