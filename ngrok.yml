version: "2"
authtoken: # Add your ngrok authtoken here
tunnels:
  web:
    proto: http
    addr: 3000
    subdomain: # Optional: specify a subdomain if you have a paid plan
  webhooks:
    proto: http
    addr: 3000
    inspect: true
